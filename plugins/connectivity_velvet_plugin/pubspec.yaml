name: connectivity_velvet_plugin
description: A plugin that provide elements to manage connectivity status of the device.
version: 0.0.1-dev.3
homepage: https://github.com/stratumfoundry/velvet/tree/main/plugins/http_client_velvet_plugin

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  connectivity_plus: ^6.0.3
  flutter_hooks: ^0.20.5
  flutter:
    sdk: flutter
  velvet_framework: ^0.0.1-dev.54

dev_dependencies:
  # Mandatory dependencies for make custom_lint work in monorepo
  custom_lint: ^0.6.4
  flutter_lints: ^5.0.0
  pyramid_lint: ^2.0.0
  velvet_custom_lints: ^0.0.1-dev.6

topics:
  - flutter
  - framework
