name: riverpod_velvet_plugin
description: A plugin that provide elements to manage Riverpod in Velvet.
version: 0.0.1-dev.1
homepage: https://github.com/stratumfoundry/velvet/tree/main/plugins/riverpod_velvet_plugin

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  velvet_framework: ^0.0.1-dev.54

dev_dependencies:
  build_runner: ^2.4.9
  velvet_generator: ^0.0.1-dev.18

  # Mandatory dependencies for make custom_lint work in monorepo
  custom_lint: ^0.6.4
  flutter_lints: ^5.0.0
  pyramid_lint: ^2.0.0
  velvet_custom_lints: ^0.0.1-dev.6

flutter: null

topics:
  - flutter
  - framework
